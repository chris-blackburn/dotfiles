alias ls="ls --color=auto"
alias ll="ls -l"
alias la="ll -a"
alias vi="vim"
alias vim="nvim"
alias tmux="tmux -2u"
alias svim="sudo -E vim"

# fuzzy find a project to open with tmux
#function __tmux_oproj() {
#    to
#    local ret=$?
#    #zle reset-prompt
#    return $ret
#}
#zle -N __tmux_oproj
#bindkey '^F' __tmux_oproj
bindkey -s '^F' "to^M"

# helpers for worktrees
#function __wtorg () {
#    local ret=$?
#    zle reset-prompt
#    return $ret
#}
#zle -N __wtorg
#bindkey '\ef' __wtorg
bindkey -s '\ef' "wtorg -s^M"

